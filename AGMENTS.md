# 核心开发范式与行为约束

在处理任何代码修改、功能迭代或架构调整时，必须强制遵守以下原则。这优先于任何常规的软件工程防御性习惯。

## 1. 绝对无视向后兼容

- 鼓励进行破坏性变更（Breaking Changes）。
- 在重构或修改逻辑时，不要为了兼容旧接口而保留冗余代码。
- 如果新设计导致其他地方报错，直接修改调用方代码以适配新逻辑。
- 彻底清理旧逻辑，不保留废弃接口、废弃分支或兼容包装层。

## 2. 数据库结构随时变更

- 随时可以直接增加、删除、重命名数据库表和字段，无需顾虑旧结构。
- 不要为了平滑过渡而保留废弃字段。
- 不要编写复杂的向下兼容迁移逻辑。
- 不考虑旧数据兼容。

## 3. 禁用兜底逻辑

- 保持代码简洁。
- 有问题直接抛出错误。
- 不做降级处理。
- 可以使用默认值。
